$("<%= @parent_id ? "#comment-#{@parent_id} " : '#commentable'%>.reply-target").append(
  "<%= escape_javascript(render :partial => 'comments/form', :locals => { :comment => @new_comment }) %>"
).focus();
