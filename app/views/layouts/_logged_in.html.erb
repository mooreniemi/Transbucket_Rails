<% if current_user.admin? %>
<li>
  <%= link_to 'Admin', admin_path %>
</li>
<% end %>
<li>
  <%= link_to 'Submissions', pins_path %>
</li>
<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
    Filter <span class="caret"></span>
  </a>
  <ul class="dropdown-menu" role="menu">
    <!-- TODO should only show when on the submissions page -->
    <li>
      <%= render partial: 'pins/filter_controls' %>
    </li>
  </ul>
</li>
<% unless current_user.gender == "cisgender" %>
<li>
  <%= link_to 'Add <i class="fa fa-plus-circle" style="color:#aa3311"></i>'.html_safe, new_pin_path %>
</li>
<% end %>
</ul>
<%= render partial: 'layouts/search' %>