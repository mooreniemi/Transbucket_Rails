<fieldset>
  <% if !f.object.new_record? %>
  <%= image_tag f.object.photo.url, alt: f.object.caption.presence || "Pin image" %>
  <% end %>

  <%= f.file_field :photo %>
  <%= f.label :caption, "Image Caption" %>
  <%= f.text_field :caption %>
  <%= delete_photo(f) %>
</fieldset>
