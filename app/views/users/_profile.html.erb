<h2><%= title 'Edit ' + resource_name.to_s.humanize + ' Profile' %></h2>
<div class="profile_well">
  <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= f.error_notification %>
  <div class="input-group">
    <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i> Name: </span>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="input-group">
    <span class="input-group-addon"><i class="fa fa-users"></i> Username: </span>
    <%= f.text_field :username, class: "form-control" %>
  </div>
  <div class="input-group">
    <span class="input-group-addon"><i class="fa fa-key"></i> Gender: </span>
    <%= f.select :gender_id, options_from_collection_for_select(Gender.all, "id", "name"), {}, { class: "form-control"} %>
  </div>
  <!-- small>On Transbucket, gender helps control the pronouns you are referred to with. Currently, MTF and FTM genders correspond to binary pronoun identifications. For gender neutral pronouns, please use "None" or "GenderQueer" as your gender. Future updates will allow for pronoun choice independent of your gender identification.</small -->
  <div class="input-group">
    <span class="input-group-addon"><i class="glyphicon glyphicon-envelope"></i> Email: </span>
    <%= f.email_field :email, class: "form-control" %>
  </div>
  <div class="input-group">
    <span class="input-group-addon"><i class="fa fa-lock"></i> Password: </span>
    <%= f.password_field :password, placeholder: "New Password", autocomplete: "off", class: "form-control" %>
    <%= f.password_field :password_confirmation, placeholder: "New Password Confirmation", autocomplete: "off", class: "form-control" %>
    <%= f.password_field :current_password, placeholder: "Current Password", class: "form-control" %>
  </div>
  <div>
    <%= f.submit "Update", class: "btn btn-primary pull-right" %>
    <%= button_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn' %>
    <br clear="both">
  </div>
  <% end %>
</div>