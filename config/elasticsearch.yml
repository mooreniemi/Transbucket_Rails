development:
  transport_options:
    request:
      timeout: !!integer 1000
test:
  transport_options:
    request:
      timeout: !!integer 1000
staging:
  transport_options:
    request:
      timeout: !!integer 900
production:
  transport_options:
    request:
      timeout: !!integer 600
