# Worklog 2026-09-06

## Staging validation

- Updated the staging smoke script to use the real pin form field IDs (`pin_surgeon_id`, `pin_procedure_id`) instead of the older nested names.
- Added a staging smoke path that uploads a real local fixture image, creates a pin, then edits it.
- The seed path was already adjusted earlier to use a real image fixture for future demo pins.

## Current intent

- Keep validation on `transbucket-staging` only.
- Use the smoke run to confirm image upload and create/edit behavior before any production deployment work.

## Current staging status

- The staging deploy released successfully as `v312`.
- Search indexing now fails open on the request path, so a Bonsai outage should not block pin create/edit.

## Search testing note

- For staging validation we are sharing the production Bonsai cluster, but not the production database.
- That is intentional: Elasticsearch indices are disposable and can be recreated from Postgres, while the database is the source of truth and should not be risked.
- Staging uses the `staging_pins` index prefix, so its search data stays separate from `production_pins` even on the shared cluster.

## Staging smoke result

- The staging browser smoke reached `/pins/new`, uploaded the real local fixture image, and submitted the form.
- The staging AWS config was corrected to use the known-working key pair while keeping `S3_BUCKET=transbucket-staging`.
- The next create attempt got past Paperclip/S3 and used to fail server-side with `Elasticsearch::Transport::Transport::Errors::NotFound` from Bonsai.
- That specific blocker is now mitigated in the app code; the remaining staging work is to rerun the smoke path and confirm the fallback path behaves correctly.

## Live staging auth

- The live staging account is `username=meowmeow`.
- The staging password is not the test seed default, so I reset that staging-only account to `password` via `heroku run rails runner` on `transbucket-staging`.
- The live staging smoke is now running again with the corrected staging credentials and waiting on the search assertion.

## Live staging smoke

- The live staging smoke passed with the temporary staging-only worker running: `staging smoke ok pin=240`.
- This confirms the create/edit/search flow on staging once delayed jobs are actually being processed.
- The temporary worker was only used to drain staging queue jobs for validation; it should not be left running longer than needed.

## Search prefix follow-up

- Added a prefix branch to `PinSearchQuery.all_xfields` so procedure-name partials like `phallo` and `orchi` can match `phalloplasty` and `orchiectomy`.
- Added a focused unit spec for the query shape that does not require a database.

## Newsfeed note

- `GET /newsfeed` on staging is currently failing because `PagesController#newsfeed` calls `open('https://transbucket.tumblr.com/rss#_=_')` without `open-uri`, so Ruby treats the URL as a local filename and raises `Errno::ENOENT`.
- This is separate from search and needs its own fix path if we want staging newsfeed to work again.

## Staging verification after fix

- Deployed the combined search/newsfeed fix to staging as `v317`.
- Live staging search for `orchi` now returns orchiectomy results from the real index data.
- Live `GET /newsfeed` on staging now returns `200 OK`.

## Newsfeed reset

- Replaced the Tumblr-backed newsfeed with a simple static reset page.
- The page now uses a short release-note style summary instead of rendering the old archived feed.
- Added a controller spec that confirms the page renders without reaching out to Tumblr.

## Newsfeed archive styling

- Added a short static archive list under the reset message.
- Each archive item now shows a body line with a date underneath it.
- The page stays intentionally minimal instead of pulling more Tumblr content live.

## Newsfeed copy update

- Swapped the Discord footer link to the permanent invite.
- Rewrote the newsfeed as short release notes with only user-facing changes.
- Kept the notes focused on the prefix search improvement and the community invite update.

## Procedure cleanup

- Repointed the two production `revision` pins to the canonical abdominal flap phalloplasty procedure.
- Removed the orphaned `revision` procedure rows after confirming there were no procedure comments attached to them.

## Procedure dedupe follow-up

- Extended the reversible `procedure:dedupe` rake task to fold the obvious `double incision` free-nipple-graft spellings into the existing canonical `double incision with grafts` row.
- Kept the liposuction-specific variants out of this pass so the next cleanup round can review those separately instead of collapsing them automatically.
- Added the `ul` abbreviation as a reversible merge into the canonical `urethral lengthening` procedure row.
- Folded the orphaned `nipple grafts` rows into the canonical `double incision with grafts` procedure, since that label is a partial technique description rather than a standalone operation in this site taxonomy.
- Added a newsfeed entry describing the procedure-name cleanup in user-facing terms so the site explains why search and browsing are now more consistent.
- Removed the five remaining zero-pin procedure rows from production, including the single-comment `nerve pedicle transfer` row and its comment, after deciding the comment was too generic to preserve.
- Started a reversible surgeon dedupe pass for the obvious whitespace/case duplicates and the `Mcevenue` / `McEvenue` spelling variant, with the plan to move pins and skills before deleting the source rows.
