# Worklog 2026-09-08

## Localization and international SEO

- Reviewed the Search Console country export and grouped the audience into language opportunities rather than treating countries as one-to-one language signals.
- The strongest non-English opportunities are German, Portuguese, Spanish, French, Dutch, Polish, Russian, Italian, Japanese, Turkish, Chinese, Vietnamese, and Arabic, alongside the existing English audience.
- Added a first-pass locale set for the missing language targets while preserving the existing German, Spanish, French, Italian, Japanese, and Chinese locale data.
- Added locale-prefixed URL selection, Accept-Language detection, and an English fallback for unsupported values.
- Added a language selector to the shared footer and `lang` attributes to rendered HTML.
- Added `hreflang` alternate links for supported locales using locale-prefixed paths.
- Preserved page caching for public static pages because the cache key includes the full locale-prefixed request path; kept the user-specific bookmarks action uncached.
- Switched from query-string locale URLs to locale-prefixed routes, with legacy GET redirects to `/en/...`, query preservation, and no redirect behavior for form submissions.
- Reviewed first-pass medical wording against WHO and WPATH terminology; public copy now prefers “gender-affirming”/locale-appropriate equivalents over literal “transition procedure” phrasing.
- Kept both “transition” and “gender-affirming” in the English public SEO copy so familiar search language and current clinical terminology are both represented.
- First-pass translations cover public navigation, homepage copy, footer labels, metadata description, and locale names. Procedure names, surgeon names, comments, and other user-generated content remain unchanged.
- Localized the release-notes newsfeed across all supported locales without changing its four user-facing entries.
- Updated `script/staging_smoke.rb` to exercise locale-prefixed staging paths directly; `STAGING_LOCALE` defaults to English and can be overridden for a localized smoke run.
- Deployed commit `4366b674` to staging as Heroku release `v328`; the build completed successfully on Heroku-24 and the staging web dyno is up.
- Verified staging GET redirects for `/` and query-locale procedure URLs, localized German homepage/newsfeed HTML, canonical metadata, and `x-default` hreflang output. The authenticated `script/staging_smoke.rb` remains ready to run with `STAGING_PASSWORD` supplied through the shell; no credential is stored in the repository.
- Localized the public directory, filter, account, submission-form, comment, procedure, surgeon, and individual pin-page labels while preserving user-generated procedure names, surgeon names, captions, comments, and details. Filter labels translate for display but submit canonical scope values.
- A pins-index title interpolation typo caused a staging `500` on localized authenticated `/pins` requests; it was fixed in `c90c6945`, covered by rendered-view controller specs, and deployed with the public-view localization as `v333` (`5009f2d2`). Anonymous checks now correctly return localized sign-in redirects instead of `500`.
- The locale-data spec and ERB parsing pass. DB-backed rendered controller specs require local Postgres access; this shell currently receives an OS permission error connecting to the Docker-mapped test port, so the authenticated staging smoke remains the final end-to-end check.
- Exhaustive anonymous staging checks across all 15 supported locales passed for homepage, newsfeed, about, terms, privacy, procedures, surgeons, contact, login, registration, and protected pins redirects. HTML language, canonical, hreflang, legacy redirects, and unsupported-locale behavior also passed.
- Found and fixed a locale cache-key issue: pin, procedure, surgeon, list-card, and comment fragments were cached without the locale, allowing English fragments to appear on localized pages after an English request. Added localized pagination translations and required-key coverage. The cache/pagination fix is local until the next staging deploy.
- Remaining deliberate scope: legal-page bodies are still English pending human-reviewed translations; account/settings/contact/procedure-creation forms contain additional public UI strings still to localize. Do not treat the locale work as complete until those surfaces and the authenticated smoke flow are checked.
- Added a translated notice to About, Privacy, and Terms explaining that the legal text is currently English-only, machine translations are intentionally not provided because they may contain errors, and the English version controls. Focused locale/request/page specs pass after this change.
- AI disclosure recommendation: if adopted, describe only AI-assisted code/documentation maintenance and state that user accounts, submissions, images, comments, and other user-provided content are not intentionally submitted to AI tools. Do not promise that no user data reaches third parties generally; hosting, storage, email, search, analytics, and advertising providers remain separate privacy-policy disclosures. Legal wording requires human review before production.
- The country export is local scratch data and remains ignored under `/data/`; exact traffic counts are intentionally not recorded here.
