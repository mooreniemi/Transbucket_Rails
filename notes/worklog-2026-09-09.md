# Worklog 2026-09-09

## Signup localization validation

- Fixed signup and login views to use the complete locale-aware `public.auth` and `account_menu` catalogs instead of the English-only `public_extra` and Devise view fallbacks.
- Added reviewed signup explanations for Portuguese (Brazil), Dutch, Polish, Russian, Turkish, Vietnamese, and Arabic. Existing locale translations remain in their locale catalogs.
- Removed the duplicate German `account_menu` and `explanations` block from `de.yml`; German account labels now have one owner in `directory.yml`.
- Extended `script/staging_smoke.rb` to verify a locale-specific registration marker and normalize UTF-8 response bodies, fixing a Japanese assertion failure in the test script.
- Added request-spec coverage asserting signup translation keys exist across all 15 supported locales.
- Deployed commit `2d838c62` to staging as Heroku release `v342` only. The full authenticated smoke flow passed for all 15 supported locales, including registration, login, two-image submission, edit, and search.
- Temporarily ran one staging worker for asynchronous indexing during the smoke run, then scaled it back to zero. Production was not touched.
- Local DB-backed RSpec execution remains blocked because PostgreSQL is not running locally; locale YAML parsing and smoke-script syntax validation passed.
