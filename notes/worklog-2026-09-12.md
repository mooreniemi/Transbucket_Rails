# Worklog 2026-09-12

## Swedish production rollout

- Captured a production Postgres backup before deployment.
- Promoted the tested `master` commit to the production `main` deploy target. Heroku released v399 successfully on Heroku-24 with Ruby 3.1.6.
- Verified the public Swedish homepage and procedure directory return successfully with `lang="sv"`, Swedish navigation, locale-prefixed links, and localized structured-data routes.
- Imported the reviewed procedure-name translations in a production one-off dyno: 155 translations imported and affected pins reindexed. The Postgres backup remains the durable rollback protection; the task's one-off dyno backup file is not durable after the dyno exits.
- Production process types remain unchanged: one web dyno is running and no worker dyno was started.
- Staging had already passed authenticated Swedish and German smoke flows with the temporary worker scaled back to zero afterward.
- Local focused mailer coverage passed (6 examples); the full suite had one pre-existing/flaky JavaScript account-cancellation failure that passed in isolation, plus 5 pending examples.
