# Worklog 2026-09-13

## Production login CSRF regression guardrails

- Confirmed the production login failure was caused by callback order: `set_locale` could authenticate valid credentials through locale capture before Rails verified the submitted CSRF token; Devise then cleared the session token and Rails returned 422. Locale-prefixed and legacy login paths reproduced the same behavior, so locale was not the cause.
- Kept the application fix at `19fd3bcb`, moving `protect_from_forgery` below the three prepended routing/locale filters so CSRF verification runs first. Staging remains configured with host-only cookies; production still requires `SESSION_COOKIE_DOMAIN=.transbucket.com` before deployment so users do not need to clear cookies.
- Added a real Capybara login regression with `ActionController::Base.allow_forgery_protection` enabled and restored in `ensure`, plus a callback-order assertion. Targeted result: 7 examples, 0 failures.
- Tightened `script/staging_smoke.rb` to require the expected authenticated redirect and distinguish a 422 CSRF rejection from invalid credentials. The authenticated staging smoke passed against release v419 (`staging smoke ok locale=en pin=363`) with a temporary worker, which was scaled back to zero immediately.
- Added a pre-production release gate to the README. Staging credentials remain shell-only and are excluded from CI and deployment configuration. New Relic alerting is documented as optional only if available at no incremental cost; no paid monitoring was added.
- The non-browser local suite passed 150 examples with 0 failures and 3 expected pending. A full all-spec run encountered 24 Capybara failures because multiple JavaScript examples attempted to bind the fixed local port 9887 concurrently; this is a local test-runner limitation, while the targeted login feature and relevant non-browser suite pass.
- Deployed the squashed Rails 4 fix as production release v427 with maintenance mode enabled, then verified Heroku release/build completion and the production web dyno. Set `SESSION_COOKIE_DOMAIN=.transbucket.com` before deployment.
- After maintenance was disabled, the apex login page returned 200 and a fresh GET-token to deliberately invalid-credential POST returned normal invalid-login 200 rather than the former CSRF 422. `www.transbucket.com` returned the expected 301 to the apex login URL. A valid production-credential authentication smoke remains to be run; no staging credential was reused and no end-to-end success is claimed without it.

## Surgeon name display cleanup

- Production surgeon records were normalized through the reversible cleanup task; remaining non-placeholder whitespace and reviewed lowercase defects were cleared. Placeholder records remain intentionally excluded for separate review.
- Found a separate display bug in `Surgeon#to_s`: Ruby `capitalize` was lowercasing the rest of mixed-case names whenever they were rendered. Changed it to preserve the curated database casing, including punctuation and multi-part names.
- Added model regression coverage while preserving the existing surgeon validation, averages, URL, and directory-name specs.
- A local RSpec run was blocked by the sandbox denying access to the local PostgreSQL port; Ruby syntax and `git diff --check` passed.
- Resumed the SEO work by adding concise, source-linked guides for the production procedures `glansplasty`, `monsplasty`, and `tracheal shave`, plus related-procedure links for each.
- Added a regression spec requiring source-backed summaries and related links for all high-priority procedure pages.
- Added source-linked guides for production `scrotectomy` and `abdominal flap phalloplasty` pages after finding those non-brand query clusters in the Search Console export.
- Verified the live compressed sitemap contains locale-prefixed URLs for the new procedure pages, and `robots.txt` advertises `https://www.transbucket.com/sitemap.xml.gz`; no manual sitemap upload is required.

## Directory sorting and local test setup

- Directory procedure and surgeon tables sort entirely in the browser across every displayed column, including authenticated rating columns; `aria-sort` now reports the actual active direction.
- Replaced per-row procedure average lookups with grouped `Pin` aggregates. Existing indexes on `pins.procedure_id` and `pins.surgeon_id` support the directory queries; no migration was needed.
- Added controller coverage for grouped averages and browser integration coverage for both sort directions and surgeon rating columns.
- Documented the canonical local workflow: Rails runs on the host while Docker supplies Postgres and Elasticsearch. The Compose override exposes Postgres as `localhost:5433`; `script/local_rspec` supplies the matching Ruby, database, and test environment defaults.
- A first browser run exposed and fixed the directory script's pre-DOM initialization bug. Focused controller specs and directory browser specs pass through the wrapper; the full suite remains the next verification gate.
- The full local suite completed with no failures. The tested branch was deployed to staging as Heroku release v402; public `/procedures` and `/surgeons` returned 200 with all procedure sort headers and the expected anonymous surgeon columns, and the deployed application asset contains the DOM-ready sorter.
- Promoted the tested HEAD `8c3eb7b4` to production as Heroku release v420 on Heroku-24. Read-only checks of `/procedures` and `/surgeons` returned 200, and production HTML confirmed the procedure table exposes sortable name, submissions, satisfaction, sensation, and discussions columns. No migration or search-index operation was part of this deploy.
- The latest local Search Console exports are dated September 12, before the production release, so they are the pre-release baseline. A later export is needed before judging SEO impact or choosing the next query cluster.

## Directory rating distributions and recent submissions

- Added visible sort icons to procedure and surgeon directory headers so the client-side sorting controls are discoverable.
- Reworked rating distribution headings to describe rating distributions rather than averages. Nonzero buckets link to authenticated, exact-rating submission filters.
- Added authenticated surgeon rating distributions overall and per procedure, using grouped queries rather than per-row database lookups.
- Added a compact preview of the three latest published submissions on authenticated procedure pages, with safe-mode-aware images and a link to each submission.
- Added localized distribution labels and a short newsfeed entry for the directory improvements.
- Focused verification passed: 29 examples, 0 failures, 2 expected pending; browser directory coverage passed 4/4. The full legacy suite still reports three unrelated feature failures in pin creation, registration, and pin updating.

## Comparison release notes

- Added two staging screenshots to the newsfeed showing procedure and surgeon comparisons with fictional demo data to protect privacy.
- Linked each release note to the relevant internal page, with locale-aware paths, and linked the Discord update to the permanent community invite.
- Newsfeed regression coverage now checks the expanded archive, both comparison images, and links for every entry.
- Local verification passed: 261 examples, 0 failures, 4 existing pending examples. No database migration or search reindex is needed for this asset/view/controller-only change.
- Staging release v427 passed the authenticated smoke (`staging smoke ok locale=en pin=368`) with the temporary worker scaled back to zero. Follow-up review changed comparison links from a parenthesized list to meaningful inline links embedded in the comparison sentence; this remains a view/controller/catalog-only change.

## Comparison scope filtering

- Fixed the optional shared-surgeon/shared-procedure selectors so they update immediately when either comparison selection changes, rather than only after submitting the form.
- The browser keeps only scopes associated with one selected entity, then intersects scopes when both entities are selected. Server-side validation and aggregate filtering remain authoritative.
- Relationship preload values use FriendlyId `to_param` values, matching the rendered select values.
- Targeted controller and browser verification passed: 25 examples, 0 failures, 2 existing pending. A subsequent full run had one unrelated flaky pin-creation browser failure; the new scope specs passed.
