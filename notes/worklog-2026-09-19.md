# Worklog: 2026-09-19

- Ran the reversible complication cleanup first pass on production after a dry-run and staging check.
- Applied 410 reviewed changes: exact placeholder tags were removed and reviewed aliases/casing were normalized.
- No narrative text was changed and no synthetic complication tags were added.
- Saved a local, gitignored affected-submission report at `data/prod-complication-cleanup-20260919.csv` containing pin IDs, internal submitter IDs, and update timestamps for follow-up.
- The broader narrative-derived candidates remain pending manual review.
- Deployed the task-only placeholder cleanup release and applied a second narrow pass on production after a dry-run: 30 pure placeholder tags were removed, including punctuation/casing variants of “none,” while meaningful compound/narrative tags were preserved.
- The cleanup remains reversible through the task backup written by the one-off dyno; no user-facing code or synthetic tags were changed.
- The next pass should review compound and multilingual entries manually, including the French urinary-complication entry, before proposing any tag split or translation.
- After deploying the matcher fix for reversed phrasing, applied one additional placeholder-only cleanup for `So far none`; a read-only production query confirmed zero remaining uses of that exact tag.
- Exported the current production complication labels for local clustering: 738 tag uses across 640 pins and 465 unique labels. Created a gitignored proposal separating high-confidence aliases from narrative and multi-complication entries that need manual review.
- Created a gitignored recurring-label review table with usage counts, proposed actions, and confidence levels; no database changes were made from the clustering review.
- Ran the reviewed second cleanup pass directly from local Ruby 3.1.6 against production: 53 reversible changes applied, including removal of `too early` and the rod/fistula correction label, reviewed alias normalization, and delayed-healing normalization. Bonsai documents were reindexed; the local backup is `data/prod-complication-cleanup-pass2-20260919.yml`.
- Read-only verification found 735 complication tag uses across 637 pins and 454 unique labels, with zero remaining uses of the old reviewed labels.
- Corrected the cleanup task after verifying acts-as-taggable-on was reusing case-insensitive legacy tag rows instead of replacing them. Replaced taggings explicitly, then applied 29 remaining case-normalization changes and 9 `Wound separation` case changes directly from local code; each pass was backed up and reindexed.
- The next clustering pass should split clearly compound labels such as `fistula and stricture` into canonical tags while preserving qualifiers in the original submission content.
- Applied the approved full-review mapping with a per-pin YAML backup and reindexed the affected submissions. The production database changes remain reversible; the original narrative text was not edited.
- Normalized all active complication labels to lowercase and reindexed 27 affected pins. A read-only audit found no remaining mixed-case labels or case-only collisions.
- Corrected three `ecchymose` taggings to the standard `ecchymosis` label with a separate rollback file; no other complication meanings were changed in that correction.
- Exported the current production procedure catalog to `data/procedures-prod-audit-20260919.csv` and created the read-only merge-review proposal `data/procedures-prod-merge-review-20260919.csv`; no procedure data was changed.
