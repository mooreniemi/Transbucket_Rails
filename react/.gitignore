node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

*storybook.log
storybook-static
.swatchbook

# Terrazzo token build output -- regenerated from tokens/core/*.json by the
# tokens:build script (wired into predev/prebuild/prestorybook). Never
# hand-edit anything in here.
src/generated
