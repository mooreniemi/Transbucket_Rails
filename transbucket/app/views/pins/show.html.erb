<div class="row">
	<div class="span6 offset3">
		<div class="well">
		<p>
  <b>Description:</b>
  <%= @pin.description %>
</p><small><%= @pin.user.name %></small></div>
</div>
</div>

<% if current_user == @pin.user %> <%= link_to 'Edit', edit_pin_path(@pin) %> <% end %> |
<%= link_to 'Back', pins_path %>